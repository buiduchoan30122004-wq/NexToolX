# Thư mục thư viện cài đặt
node_modules/

# Thư mục build thành phẩm (sẽ build trên server hoặc đẩy trực tiếp)
dist/
build/

# Hệ điều hành & Logs
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cơ sở dữ liệu SQLite cục bộ (tránh ghi đè database trên server)
database.sqlite
database.sqlite-journal

# Khóa bảo mật và biến môi trường cục bộ
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
