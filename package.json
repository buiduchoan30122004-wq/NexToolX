{
  "name": "nextoolx-monorepo",
  "version": "1.0.0",
  "description": "Scalable AI Tools Directory Monorepo",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:admin": "npm --prefix admin run dev",
    "dev": "concurrently --kill-others \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "build:frontend": "npm --prefix frontend run build",
    "build:admin": "npm --prefix admin run build",
    "build:all": "npm run build:frontend && npm run build:admin",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix admin install",
    "test:api": "node backend/test_api.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
